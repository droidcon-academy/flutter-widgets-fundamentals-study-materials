import 'package:flutter/material.dart';

class DCDataTable extends StatelessWidget {
  const DCDataTable({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('DataTables'),
      ),
      body: Center(
          // Add DataTable() as child of this
          ),
    );
  }

  // Add getColumns
  // Add getRows
}
